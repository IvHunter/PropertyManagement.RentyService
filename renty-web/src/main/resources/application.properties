# Database connection settings
spring.datasource.url=jdbc:postgresql://localhost:5432/rentyservice
spring.datasource.username=postgres
spring.datasource.password=IvanPostgre88@

# Specify the JDBC driver
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA settings
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
logging.level.org.hibernate=DEBUG
# spring.datasource.packagesToScan=com.vanko.rentyservice.data
# spring.jpa.properties.hibernate.packagesToScan=com.vanko.rentyservice.data

# Actuator
management.endpoints.web.exposure.include=*

# Debug mode
# logging.level.org.springframework=DEBUG
# logging.level.com.vanko=DEBUG